# Plugin to gather Ceph statistics.

From Ceph Jewel onwards it uses the JSON output of `ceph` and needs the Check_MK JSON plugin available at https://github.com/HeinleinSupport/check_mk/tree/master/json
